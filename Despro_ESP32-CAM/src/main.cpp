#include <Arduino.h>
/*#include <WiFi.h>
#include "secrets.h"

#define LED 4
#define WIFI_SSID SSID
#define WIFI_PASSWORD PASS

void blinkLed();

void setup(){
  pinMode(LED, OUTPUT);

  Serial.begin(115200);
  Serial.println("serial terinisialisasi");

}

void loop(){
  blinkLed();
}

void blinkLed(){
  // Turn the LED on
  digitalWrite(LED, HIGH);
  
  // Delay for a short period to see the LED change
  vTaskDelay(500 / portTICK_PERIOD_MS);
}*/